Compact a semantic block to its summary.

This is active compaction — you choose what to shelve rather than waiting for the planner. The block's full transcript turns are replaced by its summary in the rendered context.

Usage:
- `Forget(block_idx=<idx>)` — compact to the block's existing summary.
