Inspect the current awareness and context state. Use this to see detected semantic blocks, their approximate token costs, and the undetected context tail.

Optionally pass `block_idx` to drill into a specific semantic block and preview the summary rendering that would be shown after compaction.
