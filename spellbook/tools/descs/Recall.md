Recall content from a compacted semantic block back into your awareness.

When Spellbook compacts older conversation blocks, the original content is not lost — it is shelved. Use this tool to restore detail from any compacted block. Returns the full shelved content as tool output.

Usage:
- `Recall(block_idx=<idx>)` - recall the full content of a semantic block.
